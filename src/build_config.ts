/**
 * This file hold deafult build configuration options that may be modified
 * by some external script in order to enable/disable some features, etc.
 *
 * Note that this config may be used indirectly inside wrapper function that
 * holds some logic.
 *
 * @see ../set-build-config.sh
 * @see ./Platform.ts
 */

export const ENV_SWITCHING_ENABLED = true; // Android only, used in Platform.ts ENABLE_ENV_SWITCHING
